'use strict';

const { users } = require('./sql/users');
const { product_types } = require('./sql/product_types');
const { products } = require('./sql/products');
const { payment_types } = require('./sql/payment_types')